## Installation
```bash
npm install -g gulp
```

After that, go to the starter directory and just run:
```bash
npm install
```

## Start
To the server, run:
`gulp` or `gulp serve-dev`

### Unit testing
Just run
```bash
gulp test